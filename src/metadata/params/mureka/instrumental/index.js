/**
 * Mureka Instrumental 参数入口
 */

const generateInstrumental = require('./generate-instrumental')
const queryTask = require('./query-task')

module.exports = {
  generateInstrumental,
  queryTask
}
